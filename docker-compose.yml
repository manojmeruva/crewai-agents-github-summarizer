services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./mcp_integration/generated_docs:/app/mcp_integration/generated_docs
      - ./mcp_integration/db.sqlite3:/app/mcp_integration/db.sqlite3
