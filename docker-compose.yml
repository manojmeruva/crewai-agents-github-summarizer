services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Generated reports are written here so they survive container restarts.
      - ./mcp_integration/generated_docs:/app/mcp_integration/generated_docs
